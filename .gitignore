# ---- Verilator + cocotb output -------------------------------------------
build/
sim_build/
obj_dir/
results.xml
*.vcd
*.fst
*.fst.hier
dump.vcd

# ---- Synthesis / PnR -----------------------------------------------------
runs/
*.blif
*.edif
*.sdf
*.spef
*.def.gz
*.json.gz

# ---- Analog: ngspice / xschem / magic / klayout --------------------------
*.raw
*.op
*.ic
*.log
*.sch.bak
# xschem drops these when you open an unnamed schematic/symbol. Always scratch.
untitled.sch
untitled.sym
*.sym.bak
*.sch~
*.sym~
xschem/simulation/
# xschem writes netlists to ./simulation (singular) beside the schematic when
# local_netlist_dir is set. Regenerate with: xschem --no_x -n -s -q <file>.sch
simulation/
simulations/
*.spc
*.rpt
*.out
*.xml
*.ext
*.nodes
*.res.ext
*.sim
*.lyrdb
*.gds.gz
*.oas.gz

# Magic extraction products -- regenerate with `extract all` / sak-pex.sh
*.al
*.mag.bak
# Netgen LVS reports
comp.out
netgen.log
*.lvs.report
# Layout streams. Binary, and regenerated from .mag or LibreLane -- see
# CLAUDE.md "GDS is binary, gitignored and regenerated. LFS only for final
# submission." Only the .gz forms were covered before.
*.gds
*.gds2
*.oas

# ---- Python --------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ---- Editors / OS --------------------------------------------------------
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store

# ---- Local Claude state (keep shared config, drop per-user) --------------
.claude/settings.local.json
